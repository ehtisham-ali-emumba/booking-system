import { uiStrings } from "../../constants/uiStrings";
import { Box, Heading, Container } from "./elements";

export const Virtualization = () => {
  return (
    <Container>
      <Box>
        <Heading>{uiStrings.virtualization}</Heading>
      </Box>
    </Container>
  );
};
